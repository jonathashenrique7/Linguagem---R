# Fatores 

?factor

UF <- factor(c("df", "sp", "pr", "mg"))

grau.instrucao <- factor(c("médio", "superior", "especialização"),
                         levels = c("médio", "superior", "especialização"),
                         ordered = TRUE)

# A ordem dos nívels será exibida na aba console na categoria levels 

grau.instrucao


